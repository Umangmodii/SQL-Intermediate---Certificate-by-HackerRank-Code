SET NOCOUNT ON;

SELECT A.CITY_NAME,E.PRODUCT_NAME,D.LINE_TOTAL_PRICE FROM CITY A
INNER JOIN CUSTOMER B ON A.ID = B.CITY_ID
INNER JOIN INVOICE C ON B.ID = C.CUSTOMER_ID
INNER JOIN INVOICE_ITEM D ON C.ID = D.INVOICE_ID
INNER JOIN PRODUCT E ON D.PRODUCT_ID = E.ID
go
